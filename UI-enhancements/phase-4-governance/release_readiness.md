# Release Readiness — Phase 4

Calculate readiness from story completion, blocked work, unresolved dependencies, critical/high defects, test execution, and feature completion. Store inputs, not a hard-coded display percentage; expose the calculation through a project-scoped API.
