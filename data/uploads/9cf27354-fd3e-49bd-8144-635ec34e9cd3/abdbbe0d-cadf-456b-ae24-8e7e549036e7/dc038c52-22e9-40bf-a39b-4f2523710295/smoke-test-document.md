# ReleaseMind Knowledge Base Smoke Test

This document verifies that project-scoped uploads retain the selected Product
Space and Project identifiers through parsing, chunking, and embedding.

The deployment architecture uses a shared workspace context and prevents
cross-project retrieval.
