# Reports — Phase 4

Database-derived features/stories by status, stories by feature/sprint, sprint points, defects, test results, release progress, and product-space/project progress. Filters must constrain every query by product space and project. Export uses the filtered result, never static data.
